#pragma once
#include <string>
const int WINDOW_WIDTH = 1000;
const int WINDOW_HEIGHT = 1000;

const char* const APP_NAME = "Fraktal Mandelbrota - implementacja naiwna";

// maksymalna iloœæ renderowanych poziomów fraktalu
const size_t MAX_LEVELS = 350;
// iloœæ kolorów przedstawiaj¹cych kolejne poziomy
const size_t COLORS_COUNT = 100;

const double REFRESH_FPS_MIN_TIME = 1.0;
